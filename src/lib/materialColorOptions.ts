// Deprecated: color options map removed during rollback. Keep an empty export to avoid import errors
// if any older code paths still reference it during hot reload.
export type MaterialColorOptions = Record<string, never>;
export const MATERIAL_COLOR_OPTIONS: MaterialColorOptions = {};
